[project]
name = 'WaterSaveChain-Conservation-Network'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.drought-response-coordination]
path = 'contracts/drought-response-coordination.clar'
clarity_version = 3
epoch = 'latest'

[contracts.water-usage-tracking-registry]
path = 'contracts/water-usage-tracking-registry.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
